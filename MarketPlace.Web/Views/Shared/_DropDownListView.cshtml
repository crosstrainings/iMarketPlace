@using iMarketPlace.Web.Models
@model DropDownModel
@{
    Model.List.Add(new SelectListItem()
    {
        Value = "0",
        Text = "City",
        Disabled = true
    });
    Model.List.Reverse();
}
@Html.DropDownList(Model.Name, Model.List, new { @class = "form-control registration-form-select" })
